package com.example.safestep

/**
 * A simple object to hold the user logged-in user's data.
 */
object UserRepository {
    /**
     * The user object for the current logged-in user.
     */
    var currentUser: User? = null
}
